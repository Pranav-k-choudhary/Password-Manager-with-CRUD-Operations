# Backend

Small Express backend used for development. Run:

````powershell
cd backend
npm install
npm run dev

If the default port is in use, the server will now automatically try the next available port (e.g. 4001 → 4002 → ...).

To force a specific port for a single run:

```powershell
$env:PORT=4002
npm run dev
````

```

```
